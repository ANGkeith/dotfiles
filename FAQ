1. If screen brightness not working
    1. run `xrandr --verbose` to get the identifier
    1. go to `/etc/X11/xorg.conf` 
    1. add the following

    ```
        Section "Device"
            Identifier  "<identifier>"
            Driver      "intel"
            Option      "Backlight"  "intel_backlight"
        EndSection
    ```
    
